{
  "name": "omini-translator",
  "version": "0.1.0",
  "private": true,
  "description": "Tradutor de textos estático para GitHub Pages, com MyMemory e segmentação UTF-8.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "start": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=20"
  }
}
